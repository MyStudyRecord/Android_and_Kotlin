package com.example.kotlin_study.안드로이드_UI

//안드로이드에서 화면을 그리는 방법
// XML이라는 언어를 사용
/*
XML은 뭘까
DSL Language -> Domain Specific Language
안드로이드 ui를 그리기 위해 특화된 언어
 */

//핸드폰마다 화면 크기가 다 다른데 어떻게 화면을 그려야 할까?
// 픽셀, dpi, dp 단위
// 픽셀 -> 핸드폰 화면에서 빛이 나오는 전구 ->가장 작은 단위
// dpi -> dot per inch
// 예) ldpi -> 120(1인치에 120픽셀)

//dp -> Density Independent Pixcel
// 픽셀 독립적인 단위 (어느 핸드폰에서나 해상도에따라 변하지 않고 동일하게 보임)
